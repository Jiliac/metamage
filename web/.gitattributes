# Seeded, build-critical fixture DB (generated by scripts/gen-fixtures.ts).
# Committed because it is a static import baked into the build and embeds
# network-fetched Scryfall art URLs, so it is not deterministically regenerable.
# Marked generated so GitHub collapses its diff and excludes it from language stats.
src/datasource/fixtures/data/db.json linguist-generated=true -diff
